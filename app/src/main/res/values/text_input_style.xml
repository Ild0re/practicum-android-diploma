<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestInputEditText" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TestInputEditText" parent="Base.Theme.TestInputEditText" />

    <style name="CustomOutlineBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/lightGray</item>
        <item name="android:textColorHint">@color/gray</item>
        <item name="hintTextColor">@color/blue</item>
        <item name="cursorColor">@color/blue</item>
        <item name="maxLines">1</item>
        <item name="android:ellipsize">end</item>
        <item name="boxCornerRadiusTopStart">@dimen/dim12</item>
        <item name="boxCornerRadiusTopEnd">@dimen/dim12</item>
        <item name="boxCornerRadiusBottomStart">@dimen/dim12</item>
        <item name="boxCornerRadiusBottomEnd">@dimen/dim12</item>
        <item name="boxStrokeWidth">@dimen/dim0</item>
        <item name="boxStrokeWidthFocused">@dimen/dim0</item>
        <item name="android:inputType">number</item>
        <item name="expandedHintEnabled">false</item>
    </style>
    <style name="input_text">
        <item name="android:textSize">@dimen/text16</item>
        <item name="fontFamily">@font/ys_display_regular</item>
    </style>
    <style name="input_text_hint">
        <item name="android:textSize">@dimen/text12</item>
        <item name="fontFamily">@font/ys_display_regular</item>
    </style>

</resources>
